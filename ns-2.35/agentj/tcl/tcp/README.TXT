In the directory there are the following demos:

1) simpletcp - does the Java tutorial knock knock demo
2) multicastAndTcp.tcl - a demo that combines multicast (for discovery of the nodes) and TCP for transmission of  a message
3) loopbacktcp - test for loopback connextions for TCP - deprecated